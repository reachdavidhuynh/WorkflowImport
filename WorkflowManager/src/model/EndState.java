package model;

public class EndState extends State {

	//A state that allows entire workflow instance to be created when a token enters
    TerminateWorkflowInstanceAssociatedWithToken(Token token);

}
