package model;

import java.util.ArrayList;

public class ControlBranchState {

	//Allows parrallel branching to other states. State that keeps track of all states that branch off of it.
    ArrayList<State> NextStates;
    void IncreaseTokensAndMove();
	
}
